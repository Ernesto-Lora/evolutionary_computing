function mutated = uniform_mutation(individual, mp)
l = length(individual)
mutated = individual;
for i = 1:l
    if rand < mp
        mutated(i) = 

    end
end
end

keys = ["0", "1"];
values = ["1", "0"];
negative_string = dictionary(keys, values);